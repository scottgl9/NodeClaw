# npm registry config
# GitHub Packages for scoped @scottgl9 packages
@scottgl9:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Auth token for npmjs.com publishing is set via NPM_TOKEN secret in CI
# or via `npm login` locally — do not commit tokens here
